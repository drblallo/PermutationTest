find_package(OpenCL REQUIRED)
file(GLOB SOURCES "include/*.hpp" "src/*.cpp")

ADD_LIBRARY(LIB_PT STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(LIB_PT LIB_CLUTILS OpenCL::OpenCL)
TARGET_INCLUDE_DIRECTORIES(LIB_PT PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
